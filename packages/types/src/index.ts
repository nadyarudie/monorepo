// Definisi interface untuk pengguna
export interface User {
  id: string;
  name: string;
  email: string;
}

// Definisi interface untuk produk
export interface Product {
  id: string;
  name: string;
  price: number;
}
